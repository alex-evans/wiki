To point sass to watch a file and compile it to css every time it changes you can do this:

    sass --watch {scss location}:{dest css location}
    
    example...
    sass --watch static/scss:static/css
